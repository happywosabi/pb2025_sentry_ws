# sp_vision_25 相机内参标定与距离解算精度测试指南

> **作者**: Claude Code
> **版本**: 1.0
> **日期**: 2025-12-28
> **适用于**: PolarBear RoboMaster 2025 哨兵机器人 sp_vision_25 视觉系统

---

## 📋 目录

1. [概述](#1-概述)
2. [准备工作](#2-准备工作)
3. [相机内参标定完整流程](#3-相机内参标定完整流程)
4. [标定结果验证方法](#4-标定结果验证方法)
5. [距离解算精度测试](#5-距离解算精度测试)
6. [常见问题与故障排除](#6-常见问题与故障排除)
7. [附录](#7-附录)

---

## 1. 概述

### 1.1 标定系统架构

sp_vision_25 采用**三步标定法**完成相机从像素坐标到世界坐标的完整映射：

```
步骤1: 数据采集 (capture)
  └─> 输出: 图像 + IMU四元数对

步骤2: 相机内参标定 (calibrate_camera)
  └─> 输出: camera_matrix (3×3), distort_coeffs (1×5)
  └─> 指标: 重投影误差 (期望 < 0.5 px)

步骤3: 手眼标定 (calibrate_robotworld_handeye)
  └─> 输出: R_camera2gimbal (3×3), t_camera2gimbal (3×1)
  └─> 指标: 相机安装偏角、标定板距离
```

### 1.2 距离解算原理

```
装甲板检测 (像素坐标)
  ↓
solvePnP (OpenCV IPPE算法)
  ├─> 输入: camera_matrix, distort_coeffs, 装甲板4角点
  └─> 输出: xyz_in_camera (相机坐标系)
  ↓
坐标变换 (使用 R_camera2gimbal, t_camera2gimbal)
  └─> xyz_in_gimbal = R_camera2gimbal × xyz_in_camera + t_camera2gimbal
  ↓
坐标变换 (使用 R_gimbal2world from IMU)
  └─> xyz_in_world = R_gimbal2world × xyz_in_gimbal
  ↓
球坐标转换
  └─> distance = √(x² + y² + z²)
```

**误差来源分析**：
1. **内参误差** (camera_matrix, distort_coeffs) → 影响 solvePnP 精度
2. **手眼标定误差** (R_camera2gimbal, t_camera2gimbal) → 影响坐标变换精度
3. **装甲板模型误差** (实际尺寸偏差) → 影响 solvePnP 求解
4. **IMU 零漂/噪声** → 影响云台到世界坐标系变换
5. **检测误差** (角点提取精度) → 影响 solvePnP 输入

---

## 2. 准备工作

### 2.1 硬件准备

#### 标定板规格（必须严格匹配）

| 参数 | 数值 | 说明 |
|------|------|------|
| 类型 | 对称圆点图案 | OpenCV `CALIB_CB_SYMMETRIC_GRID` |
| 列数 | 10 | `pattern_cols: 10` |
| 行数 | 7 | `pattern_rows: 7` |
| 圆心间距 | 40 mm | `center_distance_mm: 40` |
| 总尺寸 | 约 360×240 mm | 9列×6行间距 |

**标定板打印要求**：
- 使用**高质量彩色激光打印**或**专业标定板**（推荐购买成品）
- 圆点对比度高（黑色圆点 + 白色背景）
- 平整度好（建议裱在铝板或亚克力板上）
- 尺寸精度 ±0.5 mm 以内

#### 硬件连接检查

```bash
# 1. 检查相机连接（HikRobot 工业相机）
lsusb | grep HIK
# 期望输出: Bus 002 Device 003: ID 2bdf:0001 HIK

# 2. 检查 CAN 总线（IMU 通信）
ip link show can0
# 期望输出: can0: <NOARP,UP,LOWER_UP,ECHO> ...

# 3. 检查设备权限
ls -l /dev/ttyACM* /dev/bus/usb/*/*
# 确保当前用户在 dialout 和 plugdev 组
```

### 2.2 软件准备

#### 编译 sp_vision_25 包

```bash
cd ~/pb2025_sentry_ws

# 编译整个包（包含标定工具）
colcon build --packages-select sp_vision_25 \
  --cmake-args -DCMAKE_BUILD_TYPE=Release \
  --symlink-install

# 检查编译结果（标定程序位置）
ls -lh build/sp_vision_25/
# 应包含: capture, calibrate_camera, calibrate_handeye, calibrate_robotworld_handeye
```

#### 配置文件准备

```bash
cd ~/pb2025_sentry_ws/src/sp_vision_25

# 检查标定配置文件
cat configs/calibration.yaml
```

**关键参数说明**：

```yaml
# configs/calibration.yaml

pattern_cols: 10              # 标定板列数
pattern_rows: 7               # 标定板行数
center_distance_mm: 40        # 圆心间距（单位: 毫米）

# 云台坐标系到 IMU 本体坐标系的旋转矩阵
# 默认为单位阵（表示两坐标系重合），需根据实际机械结构调整
R_gimbal2imubody: [1, 0, 0, 0, 1, 0, 0, 0, 1]

# 相机参数（用于数据采集）
camera_name: "hikrobot"       # 相机类型: "hikrobot" 或 "mindvision"
exposure_ms: 3                # 曝光时间（毫秒）
gain: 10.0                    # 增益（dB）
vid_pid: "2bdf:0001"          # USB VID:PID

# CBoard 参数（IMU 通信）
quaternion_canid: 0x01        # 四元数 CAN ID
bullet_speed_canid: 0x110     # 弹速 CAN ID
send_canid: 0xff              # 发送 CAN ID
can_interface: "can0"         # CAN 接口名称
```

**⚠️ 重要提示**：
- `R_gimbal2imubody` 需要根据实际机械结构确定（咨询机械组）
- 如果云台与 IMU 坐标系不对齐，此矩阵非单位阵
- 错误的 `R_gimbal2imubody` 会导致手眼标定完全失败

### 2.3 环境设置

```bash
# 设置 CAN 总线波特率（1 Mbps）
sudo ip link set can0 type can bitrate 1000000
sudo ip link set can0 up

# 启动 CAN 监控（可选，用于调试）
candump can0
# 按 Ctrl+C 停止

# 创建数据输出目录
mkdir -p ~/pb2025_sentry_ws/src/sp_vision_25/assets/img_with_q
```

---

## 3. 相机内参标定完整流程

### 3.1 步骤 1: 数据采集 (capture)

#### 运行采集程序

```bash
cd ~/pb2025_sentry_ws

# 使用默认配置
./build/sp_vision_25/capture

# 或指定配置文件和输出目录
./build/sp_vision_25/capture \
  -c src/sp_vision_25/configs/calibration.yaml \
  -o src/sp_vision_25/assets/img_with_q
```

#### 交互操作

程序启动后会显示实时图像窗口，窗口标题为 **"Press s to save, q to quit"**。

**窗口信息显示**：
- 图像：相机实时画面
- 红色文字（左上角）：
  - `Z xx.xx` - IMU Z 轴欧拉角（Yaw，单位: 度）
  - `Y xx.xx` - IMU Y 轴欧拉角（Pitch，单位: 度）
  - `X xx.xx` - IMU X 轴欧拉角（Roll，单位: 度）
- 彩色圆点：识别到的标定板圆心（绿色线连接）

**操作指令**：
- 按 `s` 键：保存当前帧（图像 + 四元数）
- 按 `q` 键：退出程序

#### 数据采集要点

**采集姿态多样性**（至少 15-20 组数据）：

| 姿态类型 | 数量 | 说明 |
|---------|------|------|
| 正对标定板 | 3-5 张 | 距离: 1m, 1.5m, 2m, 3m |
| 左侧倾斜 | 3-5 张 | 标定板相对相机旋转 20°, 30°, 45° |
| 右侧倾斜 | 3-5 张 | 同上，方向相反 |
| 上侧倾斜 | 2-3 张 | 标定板俯仰角 15°, 30° |
| 下侧倾斜 | 2-3 张 | 同上，方向相反 |
| 组合姿态 | 3-5 张 | 同时包含旋转和倾斜 |

**关键注意事项**：

1. **标定板必须完全在视野内**
   - 所有 70 个圆点（10×7）都可见
   - 程序会自动检测，成功识别时会显示绿色连线

2. **确保 IMU 数据稳定**
   - 观察窗口左上角的 Z/Y/X 欧拉角
   - 如果数值剧烈抖动，说明 IMU 存在零漂或干扰
   - 建议在静止状态下采集数据（移动云台后等待 1-2 秒）

3. **光照条件**
   - 避免过曝或欠曝（圆点边缘清晰）
   - 避免反光（标定板表面无高光区域）
   - 可调整 `exposure_ms` 和 `gain` 参数

4. **每张图像检查**
   - 保存后终端会显示: `[1] Saved in assets/img_with_q`
   - 确认圆点识别成功（绿色连线正确）

#### 输出数据格式

每次按 `s` 会生成一对文件：

```
assets/img_with_q/
├── 1.jpg          # 图像文件（原始相机分辨率）
├── 1.txt          # 四元数文件（w x y z，空格分隔）
├── 2.jpg
├── 2.txt
...
├── 20.jpg
└── 20.txt
```

**四元数格式示例** (`1.txt`):
```
0.9998 0.0012 -0.0156 0.0087
```
- 顺序为 **wxyz**（注意不是 xyzw）
- 表示 IMU 在采集时刻的姿态（imuabs 坐标系相对于 imubody 坐标系的旋转）

---

### 3.2 步骤 2: 内参标定 (calibrate_camera)

#### 运行标定程序

```bash
cd ~/pb2025_sentry_ws

# 使用默认配置
./build/sp_vision_25/calibrate_camera src/sp_vision_25/assets/img_with_q

# 或指定配置文件
./build/sp_vision_25/calibrate_camera \
  src/sp_vision_25/assets/img_with_q \
  -c src/sp_vision_25/configs/calibration.yaml
```

#### 运行过程

程序会**逐帧处理**所有图像：

1. **显示识别结果**：
   - 窗口标题: "Press any to continue"
   - 成功识别的帧会显示绿色角点连线
   - 失败的帧不会参与标定（终端显示 `[failure]`）

2. **终端输出**：
   ```
   [success] src/sp_vision_25/assets/img_with_q/1.jpg
   [success] src/sp_vision_25/assets/img_with_q/2.jpg
   [failure] src/sp_vision_25/assets/img_with_q/3.jpg  # 这张不会用于标定
   ...
   ```

3. **标定计算**：
   - 使用 OpenCV `calibrateCamera` 函数
   - 优化算法: Levenberg-Marquardt（迭代 100 次）
   - 固定 k3 畸变系数（视场角较小时不需要）

#### 标定结果输出

程序会在终端输出 **YAML 格式结果**：

```yaml
# 重投影误差: 0.1833px
camera_matrix: [2414.9359264386621, 0, 717.26243105567414, 0, 2418.0489262208148, 582.68540529942845, 0, 0, 1]
distort_coeffs: [-0.0209453389287673, 0.15028138841073832, -0.0006517722113234505, -0.0016861906197686788, 0]
```

**参数解释**：

| 参数 | 维度 | 含义 | 典型值范围 |
|------|------|------|-----------|
| **camera_matrix** | 3×3 | 相机内参矩阵 | - |
| ├─ fx | - | X 方向焦距（像素） | 1000-3000 |
| ├─ fy | - | Y 方向焦距（像素） | 1000-3000 |
| ├─ cx | - | 主点 X 坐标（像素） | 图像宽度/2 附近 |
| └─ cy | - | 主点 Y 坐标（像素） | 图像高度/2 附近 |
| **distort_coeffs** | 1×5 | 畸变系数 [k1, k2, p1, p2, k3] | - |
| ├─ k1, k2 | - | 径向畸变系数 | -0.5 ~ 0.5 |
| ├─ p1, p2 | - | 切向畸变系数 | -0.01 ~ 0.01 |
| └─ k3 | - | 高阶径向畸变（固定为 0） | 0 |

**重投影误差解读**：
- **< 0.3 px**: 优秀（工业级标定）
- **0.3 - 0.5 px**: 良好（可用于比赛）
- **0.5 - 1.0 px**: 可接受（需检查数据质量）
- **> 1.0 px**: 较差（建议重新采集数据）

#### 结果保存

**手动复制结果到配置文件**：

```bash
# 编辑配置文件
nano src/sp_vision_25/configs/calibration.yaml
```

在文件中添加/更新以下内容：

```yaml
# 在文件末尾添加（保留注释行）
# 重投影误差: 0.1833px
camera_matrix: [2414.9359264386621, 0, 717.26243105567414, 0, 2418.0489262208148, 582.68540529942845, 0, 0, 1]
distort_coeffs: [-0.0209453389287673, 0.15028138841073832, -0.0006517722113234505, -0.0016861906197686788, 0]
```

---

### 3.3 步骤 3: 手眼标定 (calibrate_robotworld_handeye)

#### 前置检查

```bash
# 确认 calibration.yaml 已包含内参
grep "camera_matrix" src/sp_vision_25/configs/calibration.yaml
grep "distort_coeffs" src/sp_vision_25/configs/calibration.yaml

# 确认 R_gimbal2imubody 已设置
grep "R_gimbal2imubody" src/sp_vision_25/configs/calibration.yaml
```

#### 运行手眼标定

```bash
cd ~/pb2025_sentry_ws

./build/sp_vision_25/calibrate_robotworld_handeye \
  src/sp_vision_25/assets/img_with_q \
  -c src/sp_vision_25/configs/calibration.yaml
```

#### 运行过程

程序会**逐帧处理**，显示信息包括：

1. **云台欧拉角**（左上角红色文字）：
   - `yaw xx.xx` - 云台 Yaw 角（度）
   - `pitch xx.xx` - 云台 Pitch 角（度）
   - `roll xx.xx` - 云台 Roll 角（度）

2. **标定板识别结果**：
   - 绿色连线表示成功识别
   - 按任意键继续下一帧

3. **手眼标定计算**：
   - 使用 OpenCV `calibrateRobotWorldHandEye` 函数
   - 同时求解相机相对云台的位姿和标定板相对世界坐标系的位姿

#### 标定结果输出

```yaml
R_gimbal2imubody: [1, 0, 0, 0, 1, 0, 0, 0, 1]

# 相机同理想情况的偏角: yaw-1.11 pitch0.01 roll-0.06 degree
# 标定板到世界坐标系原点的水平距离: 1.20 m
# 标定板同竖直摆放时的偏角: yaw123.89 pitch14.05 roll-0.86 degree
R_camera2gimbal: [0.01928451708725664, 0.00012696140743255463, 0.99981402834802846, -0.99981344688553653, -0.0010834913551969569, 0.019284643459122196, 0.0010857382619952124, -0.99999940496346484, 0.00010604311483339372]
t_camera2gimbal: [0.13089617453251859, 0.0038468007459533785, 0.094139945222010288]
```

**参数解释**：

| 参数 | 维度 | 含义 | 单位 |
|------|------|------|------|
| **R_camera2gimbal** | 3×3 | 相机坐标系到云台坐标系的旋转矩阵 | - |
| **t_camera2gimbal** | 3×1 | 相机坐标系原点在云台坐标系中的位置 | 米 (m) |

**辅助信息解读**：

1. **相机同理想情况的偏角**:
   - 理想情况：相机光轴平行于云台 Pitch 轴，指向前方
   - 偏角表示实际安装与理想情况的差异
   - **Yaw 偏角**应 < 5°（否则检查相机是否歪斜）
   - **Pitch 偏角**应 < 3°（否则检查云台水平度）
   - **Roll 偏角**应 < 2°（否则检查相机旋转）

2. **标定板到世界坐标系原点的水平距离**:
   - 世界坐标系原点 = 云台中心（在采集数据时）
   - 此距离应与实际测量值接近（±5 cm）
   - 可用于快速验证标定结果合理性

3. **标定板同竖直摆放时的偏角**:
   - 如果标定板竖直摆放，Pitch 应接近 0°
   - 如果有倾斜，此值反映倾斜角度

#### 结果保存到机器人配置文件

```bash
# 编辑哨兵配置文件（或对应的机器人配置）
nano src/sp_vision_25/configs/sentry.yaml
```

在文件中更新以下内容：

```yaml
#####-----工业相机标定参数-----#####
R_gimbal2imubody: [1, 0, 0, 0, 1, 0, 0, 0, 1]  # 保持与 calibration.yaml 一致

# 重投影误差: 0.1833px
camera_matrix: [2414.9359264386621, 0, 717.26243105567414, 0, 2418.0489262208148, 582.68540529942845, 0, 0, 1]
distort_coeffs: [-0.0209453389287673, 0.15028138841073832, -0.0006517722113234505, -0.0016861906197686788, 0]

# 相机同理想情况的偏角: yaw-1.11 pitch0.01 roll-0.06 degree
# 标定板到世界坐标系原点的水平距离: 1.20 m
# 标定板同竖直摆放时的偏角: yaw123.89 pitch14.05 roll-0.86 degree
R_camera2gimbal: [0.01928451708725664, 0.00012696140743255463, 0.99981402834802846, -0.99981344688553653, -0.0010834913551969569, 0.019284643459122196, 0.0010857382619952124, -0.99999940496346484, 0.00010604311483339372]
t_camera2gimbal: [0.13089617453251859, 0.0038468007459533785, 0.094139945222010288]
```

**⚠️ 重要提示**：
- `sentry.yaml` 是运行时使用的配置文件
- 修改后需要重新编译（如果使用了 `--symlink-install` 则无需重新编译 YAML 文件）

---

## 4. 标定结果验证方法

### 4.1 重投影误差检查

**原理**：使用标定得到的内参，将 3D 点投影回图像平面，计算与实际检测点的距离。

**标准**：
- 优秀: < 0.3 px
- 良好: 0.3 - 0.5 px
- 可接受: 0.5 - 1.0 px
- 需重做: > 1.0 px

**如何改进**：
- 增加采集图像数量（推荐 20-30 张）
- 提高姿态多样性（覆盖更大的角度范围）
- 确保标定板完全平整
- 检查图像是否过曝/欠曝

---

### 4.2 可视化验证方法

#### 方法 1: 使用标定板验证

将标定板放置在固定位置（如 1.5 米正前方），运行测试程序观察解算结果：

```bash
cd ~/pb2025_sentry_ws

# 运行相机测试程序
./build/sp_vision_25/camera_test
```

**检查项**：
1. 标定板圆心能否正确识别
2. 使用卷尺测量实际距离，与程序输出对比

#### 方法 2: 手眼标定一致性检查

**检查相机偏角**：
- Yaw 偏角应 < 5°（目视检查相机是否水平）
- Pitch 偏角应 < 3°（目视检查相机是否俯仰）
- Roll 偏角应 < 2°（目视检查相机是否旋转）

**检查平移向量** (`t_camera2gimbal`):
- X 分量（前后）：相机在云台前方约 10-20 cm
- Y 分量（左右）：应接近 0（相机在云台中轴线上）
- Z 分量（上下）：相机在云台上方约 5-15 cm

与机械设计图纸对比，偏差应 < 2 cm。

---

### 4.3 使用 ROS2 Launch 集成测试

```bash
cd ~/pb2025_sentry_ws
source install/setup.bash

# 启动完整视觉系统（使用 sp_vision_25）
ros2 launch pb2025_sentry_bringup bringup.launch.py \
  use_sp_vision:=True \
  use_rviz:=True \
  params_file:=$(pwd)/src/pb2025_sentry_bringup/params/node_params.yaml
```

**在 RViz 中检查**：
1. 打开 RViz
2. 订阅 `/sentry_debug/image` 话题（如果启用了可视化）
3. 将装甲板放置在已知距离处（如 2m, 3m, 5m）
4. 观察距离解算结果是否准确

---

## 5. 距离解算精度测试

### 5.1 测试原理

距离解算精度 = |实际距离 - 解算距离| / 实际距离 × 100%

**测试目标**：
- 近距离（1-3m）：误差 < 5%
- 中距离（3-5m）：误差 < 8%
- 远距离（5-8m）：误差 < 10%

### 5.2 准备测试装甲板

由于标定板与装甲板尺寸不同，需使用**实际装甲板**进行测试。

**装甲板物理尺寸**（定义在 `solver.cpp:12-25`）：

| 类型 | 宽度 | 高度 | 备注 |
|------|------|------|------|
| 小装甲板 | 135 mm | 56 mm | 灯条长度固定 |
| 大装甲板 | 230 mm | 56 mm | 灯条长度固定 |

**四角点定义**（装甲板坐标系，X 轴指向外）：

```cpp
// 大装甲板
{0,  115 mm,  28 mm},  // 左上
{0, -115 mm,  28 mm},  // 右上
{0, -115 mm, -28 mm},  // 右下
{0,  115 mm, -28 mm}   // 左下

// 小装甲板
{0,  67.5 mm,  28 mm},  // 左上
{0, -67.5 mm,  28 mm},  // 右上
{0, -67.5 mm, -28 mm},  // 右下
{0,  67.5 mm, -28 mm}   // 左下
```

**⚠️ 关键**：
- 装甲板尺寸必须精确（±1 mm 以内）
- 灯条必须平行且垂直于装甲板平面
- 装甲板表面平整（不可弯曲变形）

---

### 5.3 测试步骤

#### 步骤 1: 准备测试环境

```bash
# 在地面标记距离点（使用卷尺或激光测距仪）
# 推荐测试距离: 1m, 1.5m, 2m, 3m, 4m, 5m, 6m, 7m
```

#### 步骤 2: 编写测试程序

创建测试程序 `tests/distance_accuracy_test.cpp`：

```cpp
#include <fmt/core.h>
#include <yaml-cpp/yaml.h>
#include <opencv2/opencv.hpp>
#include <fstream>

#include "io/camera.hpp"
#include "tasks/auto_aim/detector.hpp"
#include "tasks/auto_aim/solver.hpp"
#include "tools/logger.hpp"
#include "tools/math_tools.hpp"

const std::string keys =
  "{help h usage ? |                     | 输出帮助信息}"
  "{@config-path c | configs/sentry.yaml | 配置文件路径}"
  "{distance d     | 2.0                 | 实际距离（米）}";

int main(int argc, char* argv[])
{
  cv::CommandLineParser cli(argc, argv, keys);
  if (cli.has("help")) {
    cli.printMessage();
    return 0;
  }

  auto config_path = cli.get<std::string>(0);
  auto actual_distance = cli.get<double>("distance");

  // 初始化相机和解算器
  io::Camera camera(config_path);
  auto_aim::Solver solver(config_path);
  auto_aim::Detector detector(config_path);

  // 测试数据记录
  std::vector<double> measured_distances;
  std::vector<double> errors;

  cv::Mat img;
  std::chrono::steady_clock::time_point timestamp;

  tools::logger()->info("=== 距离解算精度测试 ===");
  tools::logger()->info("实际距离: {:.3f} m", actual_distance);
  tools::logger()->info("按 's' 采样，按 'q' 退出并统计");

  while (true) {
    camera.read(img, timestamp);

    // 检测装甲板
    std::vector<auto_aim::Armor> armors = detector.detect(img);

    // 在图像上绘制检测结果
    auto img_display = img.clone();
    for (auto& armor : armors) {
      // 解算距离
      solver.solve(armor);

      // 提取距离（ypd[2]）
      double measured_distance = armor.ypd_in_world[2];

      // 绘制角点
      for (size_t i = 0; i < armor.points.size(); i++) {
        cv::circle(img_display, armor.points[i], 3, cv::Scalar(0, 255, 0), -1);
        cv::line(img_display, armor.points[i], armor.points[(i+1)%4], cv::Scalar(0, 255, 0), 2);
      }

      // 显示距离
      cv::putText(
        img_display,
        fmt::format("Dist: {:.3f} m", measured_distance),
        armor.points[0],
        cv::FONT_HERSHEY_SIMPLEX,
        0.8,
        cv::Scalar(0, 0, 255),
        2
      );
    }

    cv::imshow("Distance Test - Press 's' to sample, 'q' to quit", img_display);
    auto key = cv::waitKey(1);

    if (key == 'q') {
      break;
    } else if (key == 's' && !armors.empty()) {
      // 采样
      double measured = armors[0].ypd_in_world[2];
      double error = std::abs(measured - actual_distance) / actual_distance * 100.0;

      measured_distances.push_back(measured);
      errors.push_back(error);

      tools::logger()->info(
        "[样本 {}] 解算距离: {:.3f} m, 误差: {:.2f}%",
        measured_distances.size(), measured, error
      );
    }
  }

  // 统计结果
  if (measured_distances.empty()) {
    tools::logger()->warn("未采集任何样本");
    return 0;
  }

  double mean_distance = std::accumulate(
    measured_distances.begin(), measured_distances.end(), 0.0
  ) / measured_distances.size();

  double mean_error = std::accumulate(
    errors.begin(), errors.end(), 0.0
  ) / errors.size();

  double std_dev = 0.0;
  for (double d : measured_distances) {
    std_dev += (d - mean_distance) * (d - mean_distance);
  }
  std_dev = std::sqrt(std_dev / measured_distances.size());

  tools::logger()->info("=== 测试结果 ===");
  tools::logger()->info("样本数量: {}", measured_distances.size());
  tools::logger()->info("实际距离: {:.3f} m", actual_distance);
  tools::logger()->info("平均解算距离: {:.3f} ± {:.3f} m", mean_distance, std_dev);
  tools::logger()->info("平均相对误差: {:.2f}%", mean_error);

  // 保存到文件
  std::ofstream result_file("distance_test_results.csv", std::ios::app);
  result_file << fmt::format(
    "{:.3f},{:.3f},{:.3f},{:.2f}\n",
    actual_distance, mean_distance, std_dev, mean_error
  );
  result_file.close();

  tools::logger()->info("结果已保存到 distance_test_results.csv");

  return 0;
}
```

#### 步骤 3: 编译测试程序

在 `CMakeLists.txt` 中添加（第 110 行附近，Test Programs 区域）：

```cmake
add_executable(distance_accuracy_test tests/distance_accuracy_test.cpp)
target_link_libraries(distance_accuracy_test ${OpenCV_LIBS} fmt::fmt yaml-cpp tools io auto_aim)
```

编译：

```bash
cd ~/pb2025_sentry_ws
colcon build --packages-select sp_vision_25 --symlink-install
```

#### 步骤 4: 进行测试

```bash
# 准备结果文件
echo "actual_distance,measured_distance,std_dev,error_percent" > distance_test_results.csv

# 测试 1m 距离
./build/sp_vision_25/distance_accuracy_test -c src/sp_vision_25/configs/sentry.yaml -d 1.0

# 测试 2m 距离
./build/sp_vision_25/distance_accuracy_test -c src/sp_vision_25/configs/sentry.yaml -d 2.0

# 测试 3m 距离
./build/sp_vision_25/distance_accuracy_test -c src/sp_vision_25/configs/sentry.yaml -d 3.0

# ... 依次测试其他距离
```

**操作流程**：
1. 将装甲板放置在标记的距离点
2. 确保装甲板正对相机（Yaw 角 ≈ 0）
3. 程序显示实时检测画面
4. 按 `s` 键采样（建议每个距离采样 10-20 次）
5. 按 `q` 键退出，程序自动统计并保存结果

#### 步骤 5: 分析结果

```bash
# 查看结果文件
cat distance_test_results.csv
```

示例输出：

```
actual_distance,measured_distance,std_dev,error_percent
1.000,1.023,0.015,2.30
2.000,2.041,0.023,2.05
3.000,3.087,0.034,2.90
4.000,4.152,0.051,3.80
5.000,5.203,0.072,4.06
```

**绘制误差曲线**（可选，使用 Python）：

```python
import pandas as pd
import matplotlib.pyplot as plt

# 读取数据
df = pd.read_csv('distance_test_results.csv')

# 绘制误差曲线
plt.figure(figsize=(10, 6))
plt.errorbar(
    df['actual_distance'],
    df['measured_distance'],
    yerr=df['std_dev'],
    fmt='o-',
    capsize=5,
    label='Measured'
)
plt.plot(
    df['actual_distance'],
    df['actual_distance'],
    'r--',
    label='Ideal (y=x)'
)
plt.xlabel('Actual Distance (m)')
plt.ylabel('Measured Distance (m)')
plt.title('Distance Estimation Accuracy')
plt.legend()
plt.grid(True)
plt.savefig('distance_accuracy.png', dpi=300)
plt.show()

# 绘制相对误差
plt.figure(figsize=(10, 6))
plt.plot(df['actual_distance'], df['error_percent'], 'o-')
plt.axhline(y=5, color='r', linestyle='--', label='5% threshold')
plt.xlabel('Actual Distance (m)')
plt.ylabel('Relative Error (%)')
plt.title('Distance Estimation Relative Error')
plt.legend()
plt.grid(True)
plt.savefig('distance_error.png', dpi=300)
plt.show()
```

---

### 5.4 误差分析与优化建议

#### 典型误差模式

| 现象 | 可能原因 | 解决方法 |
|------|----------|----------|
| 所有距离偏大 10-20 cm | `t_camera2gimbal` 的 X 分量偏大 | 重新手眼标定或手动调整 |
| 近距离准确，远距离偏大 | 装甲板尺寸测量偏小 | 使用游标卡尺精确测量装甲板 |
| 标准差较大（> 5 cm） | 角点检测不稳定 | 改进光照条件，调整检测阈值 |
| 误差随距离线性增长 | 焦距 fx/fy 偏差 | 增加标定数据多样性，重新内参标定 |
| 某一角度误差大 | 标定姿态不够多样 | 补充该角度范围的标定数据 |

#### 优化建议

1. **提高内参标定精度**
   - 增加标定图像数量（推荐 25-30 张）
   - 确保姿态覆盖整个工作空间
   - 使用高质量标定板（专业激光雕刻）

2. **验证装甲板物理尺寸**
   ```bash
   # 使用游标卡尺测量
   - 灯条长度：56.0 ± 0.5 mm
   - 小装甲板宽度：135.0 ± 0.5 mm
   - 大装甲板宽度：230.0 ± 0.5 mm
   ```

3. **改进角点检测精度**
   - 调整二值化阈值（`threshold`）
   - 改进光照条件（避免过曝/欠曝）
   - 使用亚像素角点优化（`cv::cornerSubPix`）

4. **手动微调外参**（高级用户）
   - 如果误差呈现系统性偏差（如恒定偏移 10 cm）
   - 可在 `sentry.yaml` 中手动调整 `t_camera2gimbal`
   - 建议每次调整幅度 < 1 cm

---

## 6. 常见问题与故障排除

### 6.1 数据采集问题

#### 问题 1: 标定板识别失败

**症状**：窗口不显示绿色连线

**可能原因**：
1. 光照不足或过强
2. 标定板不完全在视野内
3. 标定板圆点模糊
4. 标定板尺寸与配置不符

**解决方法**：
```bash
# 调整曝光和增益
nano src/sp_vision_25/configs/calibration.yaml
# 修改:
exposure_ms: 5    # 增加曝光时间（光照不足时）
gain: 15.0        # 增加增益（光照不足时）

# 或减小曝光（过曝时）
exposure_ms: 1
gain: 5.0
```

#### 问题 2: IMU 数据异常

**症状**：欧拉角剧烈抖动（变化 > 10°/s）

**可能原因**：
1. CAN 总线未正确配置
2. IMU 零漂
3. 电磁干扰

**解决方法**：
```bash
# 检查 CAN 总线状态
candump can0

# 重新配置 CAN 总线
sudo ip link set can0 down
sudo ip link set can0 type can bitrate 1000000
sudo ip link set can0 up

# 重启 IMU（电源复位）
```

#### 问题 3: 相机无法打开

**症状**：程序报错 "Failed to open camera"

**解决方法**：
```bash
# 检查 USB 连接
lsusb | grep HIK

# 检查权限
sudo chmod 666 /dev/bus/usb/*/*

# 添加 udev 规则（永久解决）
sudo nano /etc/udev/rules.d/99-hikrobot.rules
# 添加:
SUBSYSTEM=="usb", ATTR{idVendor}=="2bdf", MODE="0666"

# 重新加载 udev
sudo udevadm control --reload-rules
sudo udevadm trigger
```

---

### 6.2 标定结果问题

#### 问题 4: 重投影误差过大（> 1.0 px）

**可能原因**：
1. 标定板不平整
2. 图像质量差（模糊、过曝）
3. 标定姿态单一

**解决方法**：
1. 检查标定板平整度（放在平面上观察是否翘曲）
2. 删除质量差的图像，重新采集
3. 增加不同角度的图像（特别是大角度倾斜）

#### 问题 5: 手眼标定结果不合理

**症状**：
- 相机偏角 > 10°
- `t_camera2gimbal` 与实际机械结构差异 > 5 cm
- 标定板距离与实际差异 > 20 cm

**可能原因**：
1. `R_gimbal2imubody` 设置错误
2. IMU 数据质量差
3. 标定数据姿态单一

**解决方法**：
```bash
# 1. 验证 R_gimbal2imubody
# 咨询机械组确认云台-IMU 坐标系关系

# 2. 检查 IMU 稳定性
# 在 capture 时观察欧拉角是否稳定

# 3. 重新采集数据
# 确保包含多种云台姿态（Yaw: ±60°, Pitch: ±30°）
```

---

### 6.3 距离解算问题

#### 问题 6: 距离解算误差过大（> 10%）

**诊断步骤**：

1. **检查装甲板尺寸**
   ```bash
   # 使用游标卡尺测量
   灯条长度 = ? mm （期望: 56 mm）
   装甲板宽度 = ? mm （小装甲板: 135 mm, 大装甲板: 230 mm）
   ```

2. **检查角点检测精度**
   - 启用可视化：`debug.enable_visualization: true`
   - 观察角点是否准确标注在灯条四角

3. **验证内参标定结果**
   - 重投影误差是否 < 0.5 px
   - fx 和 fy 是否接近（差异应 < 1%）

4. **验证手眼标定结果**
   - 相机偏角是否合理（< 5°）
   - `t_camera2gimbal` 是否与机械结构一致

#### 问题 7: 距离稳定性差（标准差 > 5 cm）

**可能原因**：
1. 角点检测抖动
2. IMU 噪声
3. 云台机械抖动

**解决方法**：
```yaml
# 在 sentry.yaml 中启用 EKF 滤波（如果有 tracker 模块）
# 调整过程噪声和测量噪声参数
```

---

## 7. 附录

### 7.1 坐标系定义

#### 相机坐标系（Camera Frame）

- **原点**：相机光心
- **X 轴**：指向相机右侧
- **Y 轴**：指向相机下方
- **Z 轴**：指向相机前方（光轴方向）

#### 云台坐标系（Gimbal Frame）

- **原点**：云台 Yaw 轴与 Pitch 轴交点
- **X 轴**：指向云台前方（Pitch = 0 时）
- **Y 轴**：指向云台左侧
- **Z 轴**：指向云台上方

#### 世界坐标系（World Frame）

- **原点**：地面（或场地中心）
- **X 轴**：指向场地前方
- **Y 轴**：指向场地左侧
- **Z 轴**：指向天空（垂直向上）

#### 装甲板坐标系（Armor Frame）

- **原点**：装甲板中心
- **X 轴**：垂直于装甲板平面向外
- **Y 轴**：沿装甲板宽度方向（指向左侧灯条）
- **Z 轴**：沿装甲板高度方向（指向上方灯条）

---

### 7.2 关键公式

#### 相机投影模型

```
像素坐标 (u, v) → 归一化平面坐标 (x_norm, y_norm)
x_norm = (u - cx) / fx
y_norm = (v - cy) / fy

畸变校正（Brown-Conrady 模型）
r² = x_norm² + y_norm²
x_distorted = x_norm × (1 + k1×r² + k2×r⁴) + 2×p1×x_norm×y_norm + p2×(r² + 2×x_norm²)
y_distorted = y_norm × (1 + k1×r² + k2×r⁴) + p1×(r² + 2×y_norm²) + 2×p2×x_norm×y_norm
```

#### 坐标变换

```
相机坐标系 → 云台坐标系
xyz_gimbal = R_camera2gimbal × xyz_camera + t_camera2gimbal

云台坐标系 → 世界坐标系
xyz_world = R_gimbal2world × xyz_gimbal

其中：
R_gimbal2world = R_gimbal2imubody^T × R_imubody2imuabs × R_gimbal2imubody
R_imubody2imuabs 由 IMU 四元数计算得到
```

#### 球坐标转换（`tools::xyz2ypd`）

```
yaw = atan2(y, x)
pitch = atan2(z, sqrt(x² + y²))
distance = sqrt(x² + y² + z²)
```

---

### 7.3 文件路径速查表

| 用途 | 文件路径 |
|------|----------|
| 标定配置 | `src/sp_vision_25/configs/calibration.yaml` |
| 机器人配置 | `src/sp_vision_25/configs/sentry.yaml` |
| 采集程序 | `build/sp_vision_25/capture` |
| 内参标定 | `build/sp_vision_25/calibrate_camera` |
| 手眼标定 | `build/sp_vision_25/calibrate_robotworld_handeye` |
| 数据输出 | `src/sp_vision_25/assets/img_with_q/` |
| 距离解算 | `src/sp_vision_25/tasks/auto_aim/solver.cpp` |
| 数学工具 | `src/sp_vision_25/tools/math_tools.cpp` |

---

### 7.4 参数速查表

#### 标定板参数

| 参数名 | 默认值 | 说明 |
|--------|--------|------|
| `pattern_cols` | 10 | 列数 |
| `pattern_rows` | 7 | 行数 |
| `center_distance_mm` | 40 | 圆心间距（mm） |

#### 相机参数

| 参数名 | 典型值 | 说明 |
|--------|--------|------|
| `camera_name` | "hikrobot" | 相机类型 |
| `exposure_ms` | 0.8 - 5 | 曝光时间（ms） |
| `gain` | 10 - 20 | 增益（dB） |
| `vid_pid` | "2bdf:0001" | USB VID:PID |

#### 装甲板尺寸（代码中定义）

| 参数 | 数值 | 单位 |
|------|------|------|
| `LIGHTBAR_LENGTH` | 56 | mm |
| `SMALL_ARMOR_WIDTH` | 135 | mm |
| `BIG_ARMOR_WIDTH` | 230 | mm |

---

### 7.5 常用命令速查

```bash
# 编译
colcon build --packages-select sp_vision_25 --symlink-install

# 数据采集
./build/sp_vision_25/capture -c configs/calibration.yaml -o assets/img_with_q

# 内参标定
./build/sp_vision_25/calibrate_camera assets/img_with_q -c configs/calibration.yaml

# 手眼标定
./build/sp_vision_25/calibrate_robotworld_handeye assets/img_with_q -c configs/calibration.yaml

# 距离测试
./build/sp_vision_25/distance_accuracy_test -c configs/sentry.yaml -d 2.0

# ROS2 启动
ros2 launch pb2025_sentry_bringup bringup.launch.py use_sp_vision:=True use_rviz:=True
```

---

## 📞 联系与支持

如有问题，请参考：
1. 本文档故障排除章节
2. sp_vision_25 源代码注释
3. OpenCV 官方文档（相机标定）：https://docs.opencv.org/4.x/dc/dbb/tutorial_py_calibration.html
4. RoboMaster 视觉组技术文档

**文档维护**: PolarBear RoboMaster 2025 视觉组

---

**祝标定顺利！** 🎯
